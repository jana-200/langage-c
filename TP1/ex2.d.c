#include <stdio.h>
#include <stdlib.h>

int main(){ 
    for (char i = 'A'; i <= 'Z'; i++){
        printf("Caractère = '%c'      Code déc. = %d        code hexa. = %x", i,i,i);
    }
    for (char i = '1'; i <= '9'; i++){
        printf("Caractère = '%c'      Code déc. = %d        code hexa. = %x", i,i,i);
    }
    
}